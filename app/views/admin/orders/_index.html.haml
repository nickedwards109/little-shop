%table.table-striped
  %tr
    %td Order ID
    %td Order Status
    %td Order Date
    %td User
  - Order.all.reverse.each do |order|
    %tr
      %td
        = link_to "order number: #{order.id}", admin_order_path(order)
      %td
        = order.status
      %td
        = order.created_at
      %td
        = order.user.username