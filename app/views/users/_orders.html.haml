%table.table-striped
  %tr
    %td Order ID
    %td Order Status
    %td Order Date
  - @user.orders.reverse[0..4].each do |order|
    %tr 
      %td
        = link_to "order number: #{order.id}", order_path(order)
      %td
        = order.status
      %td
        = order.created_at
